# ConThings

此仓库用于保存一些常见软件的配置文件。

wezterm
nvim

## 脚本

## 配置

### tmux

下载`tmux.conf`到本地：
```bash
curl -o ~/.tmux.conf "https://raw.githubusercontent.com/Ranba/ConThings/refs/heads/main/tmux/tmux.conf"
```
然后重新启动一下`tmux`即可。

